
class Wheel:
    def __init__(self, radius: float, thickness: float, angularVelocity: float, angle: float):
        self.avel = angularVelocity
        self.angle = angle
        self.radius = radius
        self.thickness = thickness

    def update(self):
        return
